package Oops.interfaces.extendinginterfaces;

//outer most interface need to public innner one can be any we want
public class NestedInterfaces {
    public interface Inner{
        boolean isOdd(int num);
    }

}
