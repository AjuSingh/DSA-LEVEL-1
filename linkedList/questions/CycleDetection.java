package linkedList.questions;

public class CycleDetection {
 public static void main(String[] args) {
    // public class Solution {
    //     public boolean hasCycle(ListNode head) {
    //      ListNode fast = head;
    //      ListNode slow = head;
    //      while(fast!=null && fast.next!=null)
    //      {
    //       fast = fast.next.next;
    //       slow = slow.next;
    //       if(slow==fast){
    //           return true;
                //now if we want to check the length of the cycle
                //we know the both fast and slow are at same point
                // int lenght=0;
                // do {
                //     slow =  slow.next;
                //     lenght++;
                // } while (slow!=fast);
                // now return the length of the cycle;
    //       }
    //      }
    //     return false;
    //     }
        
    // }
 }   
}
